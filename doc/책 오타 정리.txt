

156p 그림에서는 테스트2 인데 글 내용은 타이틀2 라고 언급함
168p [사용자 인증 정보]는 최하단이 아닌 중단쯤에 위치함
187p toEntity()의 설명에서 "OAuthAttributes 클래스 생성이 끝났으면 같은 패키지에 SessionUser 클래스를 생성합니다." 는 toEntity의 설명이 아니라 본문에 들어갔어야할 문장인듯 - 편집실수

오타는 아니지만 4장에서 delete 기능은 테스트 코드를 작성할 수 없나?


276p character_set_server도 추가해야할듯 , 설정안하니 collation_server 'utf8mb4_general_ci' is not valid for character_set 'latin1' 에러발생

300p "현재 구동중인" -> "현재 구동 중인"  으로 

323p 제일 아래쪽에 "왼쪽 위에" => "오른쪽 위에"가 맞는듯
366p 첫줄에 () 무의미한 괄호? 오타인가 
377p 그림 10-9에는 proxy_pass http://localhost:8080; 이 빠져있음

381p real_profile이_없으면_첫 번째가_조회된다()  <- 첫 번  띄어쓰기 되어있음

