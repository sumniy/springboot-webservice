37p 코드의 레파지토리 선언에 jcenter가 빠져있다
57p ... 스프링 Bean 읽기와 생성을(어색한 문법<-이 로 변경) 모두 자동으로 설정됩니다.
89p 필자는 어노테이션 순서를 주요 어노테이션을 클래스에 가깝게 둡니다.
-> 필자는 어노테이션 순서를 정할 때 주요 어노테이션을 클래스에 가깝게 둡니다.

142p ... 첫 문장에 var index = {...} 라고 되어있지만 실제 코드에선 var main = {...} 라고 작성되어 있음 -> 뒤에서도 계속 main이라 작성되어 있음

156p 그림에서는 테스트2 인데 글 내용은 타이틀2 라고 언급함